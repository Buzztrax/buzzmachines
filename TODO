$Id$

* libtool
** avoid lib prefix
http://lists.gnu.org/archive/html/automake/2004-05/msg00016.html
http://www.gnu.org/software/libtool/manual.html#Makefile-rules
** avoid to install *.la files

* resources
** we ignore the resource files (including machine graphics)

* docs
** use asciidoc for docs?

* lost sources
** have a script that generates machine boilerplate from introspecting a win32
   machine
** any tools to disassemble a win32 dll (wine?)

* tests
** write a simple dlopen based test host
** run it over all $top_builddir/*/*/.libs/*.so
